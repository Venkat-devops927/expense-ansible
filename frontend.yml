- name: configure frontend server
  hosts: frontend
  become: yes
  tasks:
    - name: install nginx 
      ansible.builtin.package:
        name: nginx
        state: present
    - name: start nginx server
      ansible.builtin.service:
        name: nginx
        state: restarted
        enabled: true